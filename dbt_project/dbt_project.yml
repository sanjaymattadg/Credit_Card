name: transactions_lake
version: "1.0.0"
config-version: 2
profile: transactions_lake
model-paths: ["models"]
test-paths: ["tests"]
models:
  transactions_lake:
    silver:
      +materialized: table
    gold:
      +materialized: table
